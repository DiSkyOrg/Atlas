@page "/Error"
@using System.Diagnostics

<PageTitle>Error | DiSky Atlas</PageTitle>

<div class="page">
    <EmptyState Icon="triangle-alert" Title="Something went wrong">
        <p>An error occurred while processing your request. Try reloading, or head back to the overview.</p>
        @if (ShowRequestId)
        {
            <p class="caption">Request ID: <code>@RequestId</code></p>
        }
        <BbButton Variant="ButtonVariant.Secondary" Href="/">Back to overview</BbButton>
    </EmptyState>
</div>

@code {
    [CascadingParameter] private HttpContext? HttpContext { get; set; }

    private string? RequestId { get; set; }
    private bool ShowRequestId => !string.IsNullOrEmpty(RequestId);

    protected override void OnInitialized() =>
        RequestId = Activity.Current?.Id ?? HttpContext?.TraceIdentifier;
}
